<%= form_for @user do |f| %>
  <div class="form-group">
 		<div class="form-group">
    	<%= f.label :first_name %>
      <%= f.text_field :first_name, placeholder: "First Name", class: "form-control", autofocus: true %>
    </div>
    <div class="form-group">
      <%= f.label :last_name %>
      <%= f.text_field :last_name, placeholder: "Last Name", class: "form-control", autofocus: true %>
    </div>
    <div class="form-group">
      <%= f.label :current_city %>
      <%= f.text_field :current_city, placeholder: "Current City", class: "form-control", autofocus: true %>
    </div>
    <div class="form-group">
      <%= f.label :profile_image %>
      <%= f.text_field :profile_image, placeholder: "Enter profile image url", class: "form-control", autofocus: true %>
    </div>
    <div class="form-group">
    	<%= f.label :email %>
      <%= f.email_field :email, placeholder: "Email", autofocus: true, class: "form-control" %>
    </div>
    <div class="form-group" >
      <%= f.label :password %>
      <%= f.password_field :password, placeholder: "Password must be at least 6 characters", class: "form-control" %>
    </div>
    <div class="actions">
			<%= f.submit "Save", class: "btn btn-block btn-primary" %>
    </div>
  </div>
<% end %>
